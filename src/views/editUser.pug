extends base

block content 
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(type="text" name="username", placeholder="username", required, value=loggedInUser.username)
        input(type="text" name="email", placeholder="email", required, value=loggedInUser.email)
        input(type="text" name="name", placeholder="name", required, value=loggedInUser.name)
        input(type="submit" value="Update Edit")

        