extends base
include mixins/video
block content 
    form
        input(type="text", name="keyword", placeholder="Search By Title", requried)
        labe(for="search")
        input(id="search", type="submit", value="🔍")

    div 
        each video in videos 
            +video(video)
        else 
            span Not Found Video