extends base

block content 
    h4 Change Title of Video
    form(method="POST")
        input(name="title", placeholder="Video Title", value=video.title, required)
        input(name="description",placeholder="Description",  value=video.description, type="text", requried )
        input(name="hashtags", placeholder="Hashtags, Sepertate by comma", value=video.hashtags.join(),type="text", requried)
        input(value="Save", type="submit")