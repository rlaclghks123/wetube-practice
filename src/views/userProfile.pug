extends base
include mixins/video

block content 
    img(src="/"+loggedInUser.avatar_url, width="100", height="100")
    h3=user.name
    a(href="/users/edit") Edit Profile
    hr
    
    each video in user.videos
        +video(video) 
    else 
        span Now Found Video    